name: Run Script

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v2

      - name: Make run.sh executable
        run: chmod +x run.sh

      - name: Run Script
        run: ./run.sh
